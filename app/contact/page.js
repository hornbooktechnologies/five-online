import ContactScene from "./ContactScene";

export const metadata = {
  title: "Contact Us",
  description:
    "We partner with serious brands who see their website as a revenue engine, not a design expense.",
};

export default function ContactPage() {
  return <ContactScene />;
}
